# modules/steam/SCsub
Import('env')

# Указываем, где лежат заголовочные файлы Steamworks SDK
env_steam = env.Clone()
env_steam.Append(CPPPATH=['.'])

# Исходные файлы нашего модуля
steam_module_files = [
    "steam.cpp",
    "register_types.cpp",
]

# Добавляем их в список на компиляцию
env_steam.add_source_files(env.modules_sources, steam_module_files)
