Import('env')

env_discord = env.Clone()

# Путь к заголовочным файлам Steam
env_discord.Append(CPPPATH=['#thirdparty/steam'])

# if env['platform'] == 'windows':
#     env_discord.Append(LIBPATH=['#thirdparty/steam/lib'])
#     env_discord.Append(LIBS=['steam'])

env_discord.add_source_files(env.modules_sources, "*.cpp")
